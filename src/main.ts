import { bestbot } from './bot.js';

// declare const global: {
//   [x: string]: any;
// };

// global.stuff = (): void => {
//   bestbot.start();
// };


// youtube.authorize(() => bestbot.start());
bestbot.start(); 
